# React and Redux

This project was created to learn more about Redux in React projects.

## Used concepts
- Store
- Actions
- Reducers

## How to run the project
1. Install dependencies
`yarn install`

2. Run the project
`yarn start`

3. Open [http://localhost:3000](http://localhost:3000) to view it in your browser.
